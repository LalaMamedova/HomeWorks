<Window x:Class="ToDo.View.MainToDoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDo.View"
        mc:Ignorable="d"
        Name="MyWindow"
        Title="MainToDoWindow" Height="550" Width="500">

    <Window.Resources>
        <Style x:Key="MyButtonStyle" TargetType="Button">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="25"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="MyWindow1" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="17*"/>
            <ColumnDefinition Width="83*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button 
            Style="{StaticResource MyButtonStyle}"
            Content="For Home"  Name="ForHomeButton" Click="ForHomeButton_Click" Grid.ColumnSpan="2">
            <Button.Background>
                <ImageBrush ImageSource="/View/Imgs/HomeButton.png"/>
            </Button.Background>
        </Button>

        <Button Grid.Row="1"
            Style="{StaticResource MyButtonStyle}"
            Content="For Work"  Name="ForWorkButton" Click="ForWorkButton_Click" Grid.ColumnSpan="2">
            <Button.Background>
                <ImageBrush ImageSource="/View/Imgs/WorkButton.png"/>
            </Button.Background>
        </Button>

        <Button Grid.Row="2"
            Style="{StaticResource MyButtonStyle}"
            Content="Other"  Name="ForOtherButton" Grid.ColumnSpan="2">
            <Button.Background>
                <ImageBrush ImageSource="/View/Imgs/Untitled.png"/>
            </Button.Background>
        </Button>

        
        <Frame Name="Main"
               NavigationUIVisibility="Hidden" 
               Grid.Row="0"
               Grid.RowSpan="4" Grid.ColumnSpan="2"/> 
    </Grid>
</Window>
