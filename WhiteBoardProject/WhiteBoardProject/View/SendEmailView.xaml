<UserControl x:Class="WhiteBoardProject.View.SendEmailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WhiteBoardProject.View" 
             xmlns:viewmodel="clr-namespace:WhiteBoardProject.ViewModel" 
             d:DataContext="{d:DesignInstance Type=viewmodel:SendEmailViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1050" d:DesignWidth="1800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.6*"/>
            <RowDefinition/>
            <RowDefinition Height="30px"/>

        </Grid.RowDefinitions>

        <StackPanel Grid.ColumnSpan="1">
            <GroupBox Header="От" Height="55">
                <TextBox Height="25" Text="{Binding UserEmail}"/>
            </GroupBox>
            
            <GroupBox Header="Ваш пароль" Height="55">
                <TextBox Height="25" Text="{Binding UserPassword}"/>
            </GroupBox>

            <GroupBox Header="К" Height="55">
                <TextBox Height="25" Text="{Binding ReciverEmail}"/>
            </GroupBox>
            
            <GroupBox Header="Тема" Height="55">
                <TextBox Height="25" Text="{Binding MailMessage.Subject}"/>
            </GroupBox>

            <ItemsControl ItemsSource="{Binding MailMessage.Attachments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

          
        </StackPanel>
            
        <GroupBox Header="Тело сообщения"  Grid.Row="1" 
                  Grid.ColumnSpan="2">
            <TextBox Text="{Binding MailMessage.Body}"/>
        </GroupBox>

        <StackPanel Grid.Column="1">
            <StackPanel.Background>
                <ImageBrush ImageSource="/Img/Background.png"/>
            </StackPanel.Background>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Width="40" Height="30">
                <Button.Background>
                    <ImageBrush ImageSource="/Img/Attach.png"/>
                </Button.Background>
            </Button>
        </StackPanel>

        <Button Grid.Column="1" Grid.Row="2"
                Width="150" Content="Отправить"
                Command="{Binding SendMessage}"/>
    </Grid>
</UserControl>
